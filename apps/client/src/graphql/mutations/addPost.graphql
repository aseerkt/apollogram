mutation AddPost($caption: String!) {
  addPost(caption: $caption) {
    ok
    post {
      id
      caption
      imgURL
      createdAt
      updatedAt
      user {
        username
      }
    }
    error {
      path
      message
    }
  }
}
